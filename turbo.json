{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["distributions/**", ".next/**", "!.next/cache/**"]
    },
    "ci": {
      "dependsOn": ["^build"]
    }
  }
}
