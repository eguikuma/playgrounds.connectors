{
  "name": "@outcomify/restify",
  "version": "0.1.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./distributions/index.d.ts",
      "import": "./distributions/index.js"
    }
  },
  "files": [
    "distributions"
  ],
  "scripts": {
    "build": "rm -rf distributions && tsup",
    "ci": "eslint --fix . && biome lint --fix . && biome format --write . && tsc --noEmit"
  },
  "dependencies": {
    "@outcomify/requestify": "workspace:*"
  },
  "devDependencies": {
    "http-status-codes": "^2.3.0",
    "tsup": "^8.5.1"
  }
}
